guard 'livereload' do
  watch(%r{trst/views/.+\.(erb|haml|slim)})
  watch(%r{lib/.+\.rb})
  watch(%r{public/.+\.(css|js|html)})
  watch(%r{trst/i18n/.+\.yml})
  watch(%r{trst/assets/stylesheets/.+\.sass})
  watch(%r{trst/assets/javascripts/.+\.coffee})
end
