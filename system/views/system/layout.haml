!!! 5
%html
  = partial('layout/html-head')

  %body{id: "system", data: {js_module_path: Trst.firm.js_path, admin: @current_user.admin?.to_s}}
    #main.wrap
      %article#xhr_msg{class: (flash[:msg][:msg][:class] rescue 'loading')}= flash[:msg][:msg][:txt] rescue '...'
      %header#header.system
        = partial('layout/header')

      %nav#menu.system
        = partial('layout/menu')

      %section#content
        %article#xhr_content
          = yield
      %section#sidebar.system
        = partial('layout/sidebar')

      %section#main_footer.wrap

    %footer#footer.wrap
      = partial('layout/footer')
